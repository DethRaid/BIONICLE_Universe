import System.IO;
/*
//string with the name of the file. The name shouldn't change, so the variable is private.
private var dataFileName = "PlayerData.budf";
private var dataText;
//This is the array with all the lines of the file in it
private var dataLines;
private var playerName;
private var playerHealth;
private var playerEssence;
//This lets us get the data from our file. It's a data stream.
//private var playerStr = new StreamReader( "/Users/Dagon Excelstraun/Desktop/" +dataFileName );

function Start()
{
	player = transform.GetComponent(Player);
	dataText = playerStr.ReadToEnd();
	playerStr.Close();
	dataLines = dataText.Split( "|" [0] );
	var tempLines = dataLines[0].Split( "=" [0] );
	playerName = tempLines[1];
	var tempLines2 = dataLines[1].Split( "=" [0] );
	playerHealth = parseInt( tempLines2[1] );
	player.stats.health = playerHealth;
	player.stats.currentHealth = playerHealth;
	var tempLines3 = dataLines[2].Split( "=" [0] );
	playerEssence = parseInt( tempLines3[1] );
	player.stats.essencePoints = playerEssence;
	player.stats.currentEssence = playerEssence;
}*/